$schema: 'https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json'
name: Tag pill
status: stable
description: 'Atom — a small dietary/cuisine label pill.'
props:
  type: object
  required: [label]
  properties:
    label:
      type: string
      title: Label
      description: 'Text shown inside the pill.'
